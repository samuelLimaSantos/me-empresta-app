import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    backgroundColor: '#9A5CD1'
  },
  image: {
    width: 130,
    resizeMode: 'contain'
  }
});

export { styles };