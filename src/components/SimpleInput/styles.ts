import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: '100%',
    padding: 10,
    paddingLeft: 20,
    backgroundColor: '#DBDBDB',
    borderRadius: 8,
    marginTop: 20,
    marginBottom: 20
  }
});

export { styles };


