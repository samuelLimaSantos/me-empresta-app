import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: '100%',
    padding: 10,
    paddingLeft: 20,
    backgroundColor: '#DBDBDB',
    borderRadius: 8,
    marginTop: 10,
    marginBottom: 10
  }
});

export { styles };


