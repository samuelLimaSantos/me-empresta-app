import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  input: {
    width: '100%',
    padding: 8,
    paddingLeft: 10,
    height: 50,
    backgroundColor: '#DBDBDB',
    borderRadius: 8,
    marginVertical: 10
  }
});

export { styles };


